import { Request, Response, NextFunction } from 'express';

const GetAllLectureValidate = (req: Request, res: Response, next: NextFunction) => {

  
  next();
};

export default GetAllLectureValidate;